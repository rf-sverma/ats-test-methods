
//@ts-nocheck
import axiosInstance from '../axiosInstance';
   // indexOf: Gets the index of a cookie.
export const indexOf = (item) => {
    console.log('indexOf init')
    // Add your implementation here
    return cookies.data.indexOf(item);
};
